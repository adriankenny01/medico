{% extends 'base.html.twig' %}

{% block title %} Editar Usuario {% endblock %}

{% block body %} 
    <div class="content">
        <div class="row">
          <div class="col-md-12">
            <div class="card">
              <div class="card-header">
                <h4 class="card-title"> Editar Usuario </h4>
                <p class="card-category"> Here is a subtitle for this table</p>
                <a href="/user/new/" class="btn btn-primary float-right">Nuevo</a>
              </div>
                <div class="p-3">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    {{ form_end(form) }}
                </div>
            </div>
          </div>
        </div>
      </div>
{% endblock %} 